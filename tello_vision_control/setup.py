from setuptools import setup, find_packages

setup(
    name='tello_vision_control',
    version='1.0.0',
    description='Un package Python',
    author='Moi',
    packages=find_packages(),
    install_requires=[
    ],
)